import { MainBanner, MainDescription, LinkArea } from './components';

export default function Main() {
  return (
    <main>
      <section>
        <MainBanner />
        <MainDescription />
        <LinkArea />
      </section>
    </main>
  );
}
